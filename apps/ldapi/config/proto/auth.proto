syntax = "proto3";

package auth;

import "users.proto";

message AuthCheckRequest {
  string uid = 1;
  string password = 2;
}

message AuthCheckResponse {
  users.ProtoUser user = 1;
}

service AuthRpcService {
  rpc CheckCredentials(AuthCheckRequest) returns (AuthCheckResponse);
}
